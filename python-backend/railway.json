{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "ignorePaths": [
      "../package.json",
      "../package-lock.json",
      "../node_modules",
      "../.next",
      "../app",
      "../public"
    ]
  },
  "deploy": {
    "startCommand": "uvicorn main:app --host 0.0.0.0 --port $PORT"
  }
}

